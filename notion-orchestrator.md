---
name: notion-orchestrator
description: |
  《數位時代》Notion 產稿主腦。掃描「AI產稿中心 v2（Claude Code）」資料庫中「未開始」條目，自動判斷稿件類型，派發對應子代理處理，並將成品寫回 Notion。支援依負責人篩選，只處理指定成員的待辦稿件。

  請在以下情境下主動使用此 Skill：
  - 使用者說「跑今天的稿件」、「處理 Notion 的待辦」、「掃描產稿資料庫」
  - 使用者說「處理 [姓名] 名下的稿件」、「幫我跑 [姓名] 的待辦」、「[姓名] 有哪些未開始的稿件」
  - 任何需要批次處理 Notion 產稿資料庫的情境
---

# Notion 產稿主腦 — 《數位時代》

## 資料庫資訊

- **資料庫名稱**：AI產稿中心 v2（Claude Code）
- **資料庫 URL**：https://www.notion.so/a438232ce0a94fe6b70d9f2e9199a32a
- **Data Source ID**：collection://71ff4d7b-3745-4b37-9910-1ee0c0ca8a47

## 欄位說明

| 欄位 | 類型 | 說明 |
|------|------|------|
| 標題 | Title | 原始標題（通常為英文或待翻譯的來源標題） |
| 建議標題 | 文字 | 編輯建議的中文標題，子代理動筆前應優先採用 |
| 稿件類型 | 下拉 | `即時新聞` / `深度分析` / `潤稿` / `社群貼文` / `文章查核` / `教學文` |
| 負責人 | 下拉 | 先泰 / 柔瑋 / 美欣 / 若彤 / 建鈞 / 祈安 / 宗穎 / 莞淇 |
| 來源 1 | URL / 文字 | 主要來源（Obsidian 本地路徑或網址） |
| 來源 2 | URL / 文字 | 第二來源（選填，同上） |
| 來源 3 | URL / 文字 | 第三來源（選填，同上） |
| 備註 | 文字 | **人類給 AI 的明確指示，動筆前必讀，內容優先於所有預設寫法** |
| 摘要 | 文字 | AI 填入稿件摘要，**75 個全形字元以內（含標點符號）** |
| 重點一 | 文字 | AI 填入第一大重點（50 字以內） |
| 重點二 | 文字 | AI 填入第二大重點（50 字以內） |
| 重點三 | 文字 | AI 填入第三大重點（50 字以內） |
| 狀態 | Status | `未開始` → `進行中` → `完成` / `失敗` |
| 完成時間 | 日期 | AI 完成後填入當天日期 |
| 社群貼文 | 文字 | AI 產出的社群貼文內容（僅 `社群貼文` 類型填入） |
| 字數 | 數字 | 稿件全文的字數，完成後由 AI 填入 |
| 建立時間 | 日期 | 自動記錄，不需填寫 |

---

## 執行流程

### Step 1：掃描待處理條目

使用 `Bash` 工具直接呼叫 Notion API，以屬性條件精確篩選，不受頁面是否有內文影響。MCP 的 `notion-search` 不支援結構化 filter，無法用於這個批次查詢場景，所以查詢階段保留 curl。**這是整個流程唯一接觸 NOTION_TOKEN 的地方，Step 2 起一律改用 MCP（見下方 callout）。**

**Token 安全規範（必讀）：**

- Token 存於 `~/.claude/settings.local.json` 的 `env.NOTION_TOKEN`
- **禁止** `export NOTION_TOKEN=...`：會把 token 留在整個 Bash session 的環境變數中，後續 tool result 與背景指令都可能洩漏
- **禁止** `echo $NOTION_TOKEN`、寫入暫存檔、`set -x` 或 `bash -x` 執行 curl：會把 token 印到 stdout / stderr，進入 transcript
- **禁止**把 token 字面值貼入 prompt 或記憶
- **唯一允許的模式**：**inline command substitution**——把 `$(jq -r ...)` 直接寫在 curl 的 `-H "Authorization: Bearer ..."` 參數裡。token 只在 fork 出去執行 curl 的那一瞬間存在，呼叫結束即釋放，不污染 session、不進 shell history。
- ⚠️ **不可使用 `NOTION_TOKEN=$(...) curl -H "...$NOTION_TOKEN..."` 寫法**：在 zsh/bash 中，`VAR=val cmd args` 的 args 是由**父 shell** 展開、而非繼承 cmd 的環境，所以 `$NOTION_TOKEN` 會被展開成空字串，導致 curl 永遠 401。此 bug 已踩過坑（2026-05-07），切勿再犯。

**兩種篩選模式：**

| 使用者指令 | curl 指令中的 filter |
| ----------- | --------- |
| 「跑今天的稿件」、「處理所有待辦」等通用指令 | 僅篩選 `狀態 = 未開始` |
| 「處理先泰的稿件」、「跑柔瑋的待辦」等指定負責人指令 | `狀態 = 未開始` AND `負責人 = [指定姓名]` |

**通用模式（所有未開始稿件）：**

```bash
curl -s -X POST "https://api.notion.com/v1/databases/a438232ce0a94fe6b70d9f2e9199a32a/query" \
  -H "Authorization: Bearer $(jq -r '.env.NOTION_TOKEN' ~/.claude/settings.local.json)" \
  -H "Notion-Version: 2022-06-28" \
  -H "Content-Type: application/json" \
  -d '{
    "filter": {
      "property": "狀態",
      "status": { "equals": "未開始" }
    }
  }'
```

**指定負責人模式（例如先泰）：**

```bash
curl -s -X POST "https://api.notion.com/v1/databases/a438232ce0a94fe6b70d9f2e9199a32a/query" \
  -H "Authorization: Bearer $(jq -r '.env.NOTION_TOKEN' ~/.claude/settings.local.json)" \
  -H "Notion-Version: 2022-06-28" \
  -H "Content-Type: application/json" \
  -d '{
    "filter": {
      "and": [
        { "property": "狀態", "status": { "equals": "未開始" } },
        { "property": "負責人", "select": { "equals": "先泰" } }
      ]
    }
  }'
```

負責人選項：`先泰` / `柔瑋` / `美欣` / `若彤` / `建鈞` / `祈安` / `宗穎` / `莞淇`

**進行中空白頁回收（防漏，2026-06-01 新增）：**

只篩「未開始」會漏掉一種狀況：上一輪已把狀態標成「進行中」卻沒成功寫回內文的孤兒頁（產稿中斷、寫回失敗）。這類頁面狀態已不是「未開始」，下次掃描被跳過，內文卻是空的，等於稿子人間蒸發。**此坑已踩過（2026-06-01，先泰名下五篇全卡在「進行中」空白頁）。**

因此掃描時，除上述「未開始」filter 外，**一律加掃「進行中」條目**（通用模式：`狀態 = 未開始` OR `狀態 = 進行中`；指定負責人模式：`負責人 = [姓名]` AND（`狀態 = 未開始` OR `狀態 = 進行中`））。對每筆「進行中」條目，用 `notion-fetch` 檢查頁面內文：

- **內文為空（`blank-page`）或摘要／重點屬性全空** → 判定為上一輪失敗的孤兒頁，併入待處理清單重跑
- **內文已有完整稿件** → 真的處理中或已完成，跳過不動

回報待處理清單時，標明哪幾筆是「未開始」、哪幾筆是「進行中孤兒頁回收」，讓使用者知道曾發生漏寫。

API 回傳 JSON，從每筆結果的 `properties` 欄位讀取：標題（`標題.title[0].plain_text`）、負責人（`負責人.select.name`）、稿件類型（`稿件類型.select.name`）、來源（`來源 1.url`、`來源 2.url`、`來源 3.url`）、備註（`備註.rich_text[0].plain_text`）、建議標題（`建議標題.rich_text[0].plain_text`）、頁面 ID（`id`）。

篩選後列出待處理清單，並在開始前告知使用者：「找到 N 筆待處理稿件（負責人：[姓名 或 全部]），開始處理。」

若沒有符合條件的條目，直接回報：「目前無待處理稿件。」並結束流程。

---

> **🔒 從 Step 2 起一律使用 MCP（`mcp__claude_ai_Notion__*`）**
>
> 所有逐頁操作（fetch 內文、更新屬性欄位、寫回稿件全文、改狀態）一律走 MCP 工具：
> - `mcp__claude_ai_Notion__notion-fetch` — 讀單頁
> - `mcp__claude_ai_Notion__notion-update-page` — 改屬性、取代頁面內文
> - `mcp__claude_ai_Notion__notion-create-pages` — 建立新頁
>
> **禁止 Step 2 之後再呼叫 curl + Notion API**。原因：
> 1. MCP 自動處理 markdown ↔ block 雙向轉換，避免手動建 block JSON（5/6 寫回時用 heading_2 + bullet 而非 paragraph 行內粗體，正是手動 block 結構的踩坑案例）
> 2. MCP 回應已預處理為 markdown，token 比 block JSON 省 3-4 倍
> 3. Token 不再進入 shell process，安全層級提升一級
>
> 唯一例外：Step 1 的批次查詢（MCP 無對應工具）。

---

### Step 2：並行處理所有條目

#### 2a. 批量標記為「進行中」

對所有待處理條目，**依序逐一**呼叫 `mcp__claude_ai_Notion__notion-update-page`，將每筆的 `狀態` 改為「進行中」（此步驟仍循序，避免 Notion API 頻率限制）。

#### 2b. 判斷稿件類型

**判斷優先順序：**

1. `稿件類型` 欄位有值 → 直接採用
2. `稿件類型` 為空，但 `備註` 有說明 → 從備註內容推斷
3. 兩者皆空 → 依下方判斷規則自動推斷

**各類型判斷規則：**

| 稿件類型 | 判斷依據 | 使用子代理 |
|---------|---------|-----------|
| `即時新聞` | 來源為主流媒體網址（彭博社、路透社、CNBC、紐時、The Verge、中央社、BBC 等新聞媒體） | `news-daily` |
| `深度分析` | 來源為頁數較多的 PDF 檔，或部落格平台長文（Substack、Medium 等），內容具強烈分析性或解釋性，聚焦商業模式或技術迭代洞察 | `deep-analysis` |
| `潤稿` | 來源為一篇已完整的文章（通常備註或標題已標示「潤稿」） | `draft-polish` |
| `教學文` | 來源為 X（Twitter）創作者貼文、方法論分享，或任何以「教讀者如何做某件事」為核心的工具教學素材 | `tutorial-article` |
| `社群貼文` | 來源為數位時代文章連結，任務為將內文轉化為社群貼文 | `social-post` |
| `文章查核` | 來源為一篇需核對事實的文章（日期、人物、數字、事件順序等） | `article-checker` |

**模糊情境處理：**

- **深度分析 vs 教學文難以判斷時**（例如：部落格長文同時具備分析性與教學性）→ **暫停，主動詢問使用者**，說明文章特徵並請確認要用哪種類型，不自動猜測
- **其他無法判斷的情況** → 預設使用 `news-daily`，並在頁面內文開頭標註 `[自動判斷為即時新聞，請人工確認]`

#### 2c. 並行派發所有子代理

所有條目完成類型判斷後，**在同一則訊息中一次呼叫所有 `Agent` 工具**，同時啟動所有子代理並行處理。

使用 `Agent` 工具，依判斷結果派發對應的子代理（`subagent_type` 填入對應 skill 名稱）。

**來源預讀（orchestrator 負責）：**

派發子代理前，orchestrator 先逐一檢查每個來源欄位：

| 來源格式 | 判斷方式 | 處理 |
|---------|---------|------|
| **本地路徑** | 以 `/` 或 `~/` 開頭，副檔名為 `.md` | orchestrator 用 `Read` 讀取檔案內容，將全文直接寫進子代理 prompt。若 Read 失敗（檔案不存在、路徑錯誤），視同來源失敗，記錄失敗原因，不傳給子代理 |
| **URL（優先匹配 inbox/）** | 以 `http://` 或 `https://` 開頭 | **先掃 `~/Claude Project/inbox/` 所有 md 檔案的 frontmatter `source` 欄位**。匹配策略：嚴格相等優先；其次比對 hostname + path（忽略 query string、fragment、尾端斜線）。命中 → `Read` 本機檔案全文，寫進子代理 prompt（附上原始檔名供溯源）。未命中 → 將 URL 傳給子代理，由子代理自行 web_fetch |
| **空白** | 欄位無值 | 跳過 |

**為什麼優先匹配 inbox/：** Terry 透過 Obsidian Web Clipper 抓下的素材，frontmatter `source` 即為原始網址，等於「網址 → 本機檔案」自動對照表。本機讀取無 fetch 失敗風險、無付費牆問題，且避開官方網站偶發的反爬限制。

傳給子代理的 prompt 包含：
- 原始標題
- 建議標題（若有，子代理應優先以此為稿件標題）
- 來源 1（必填）、來源 2、來源 3（選填，有則一併傳入）。若為本地路徑，傳入的是 **已讀取的全文內容**（附上原始檔名供溯源），不是路徑本身
- **備註（人類指示）**：若備註欄有內容，視為編輯的明確指令，**必須在動筆前先讀完，並在整篇稿件中落實**。備註優先於所有預設寫法。

子代理負責讀取來源內容並產出完整稿件（本地來源已由 orchestrator 預讀，子代理僅需處理 URL 來源）。

**關鍵規則：來源讀取失敗時，停止而非硬寫。**

子代理的 prompt 中必須包含以下指令：

> 你必須成功讀取至少一個主要來源的完整原文，才能開始撰稿。如果所有來源都無法讀取（web_fetch 失敗、403、付費牆、逾時），**不要使用其他二手來源改寫**，直接回傳以下格式：
> `SOURCE_FAILED: [來源 URL] — [失敗原因]`
> 不產出任何稿件內容。用不完整素材硬寫會導致引言被改寫、數據無法核實等事實錯誤。

**付費牆媒體（Bloomberg、WSJ、日經、Business Insider）同樣適用此規則。** 不使用 Playwright 備援。

#### 2c-bis. 來源失敗彙整與補件

所有子代理回傳後，檢查是否有 `SOURCE_FAILED` 回報。

- **有失敗項目**：將所有失敗的稿件彙整，一次向使用者索取原文。格式如下：

```
📎 以下稿件的來源無法讀取，請提供原文（可用 Dia 瀏覽器擷取 Markdown 貼入）：

1. 《標題一》— Bloomberg 付費牆
2. 《標題二》— 來源回傳 403
```

使用者提供原文後，重新派發對應的子代理，將原文直接寫在 prompt 中傳入。

- **全部成功**：直接進入下一步。

#### 2d. 事實查核（自動套用確認修改）

所有稿件產出完成後（含補件重跑的），寫回 Notion 之前，對每篇稿件執行事實查核。

**執行方式：** 使用 `Agent` 工具，`subagent_type` 填入 `article-checker`，將完整稿件內容傳入，要求三層查核（錯字、邏輯、事實）與貨幣換算。可並行啟動所有查核代理。

**查核結果處理：**

| 查核結果類型 | 處理方式 |
|---|---|
| 確認有誤且有明確正確答案（如錯誤稱謂、錯誤名稱、引言與原文不符） | **直接套用修改** |
| 數據或引言無法從來源核實 | 在該段落後加上 `[編按：此數據/引言在標註來源中無法找到，請確認出處或考慮移除]` |
| 來源連結失效或缺失 | 在資料來源區標註 `[編按：請補完整連結]` 或 `[編按：連結已失效，建議移除]` |
| 無問題 | 不動 |

查核完成後，使用套用修改後的版本進入下一步寫回。

#### 2e. 寫回 Notion

所有稿件查核完成後，**依序逐一**寫回（此步驟仍循序，避免 Notion API 頻率限制）。每篇分兩步寫回：

**步驟一：用 `mcp__claude_ai_Notion__notion-update-page`（replace_content 模式）寫入頁面內文**

頁面內文採以下精確結構（即時新聞、深度分析、潤稿、文章查核共用）：

```
**重點一**：xxx
**重點二**：xxx
**重點三**：xxx
---
**摘要**：xxx
---
（正文）
```

Block 規格：
- 3 個 `paragraph`，行內粗體「**重點一**：」「**重點二**：」「**重點三**：」開頭
- **不要**用 `bulleted_list_item`、**不要**加 `## 三大重點` 這種 `heading_2` 小標
- 1 個 `divider`
- 1 個 `paragraph`，行內粗體「**摘要**：」開頭
- 1 個 `divider`
- 正文（保留各段小標 `heading_2` / `heading_3`）

**例外（教學文 tutorial-article）**：教學文章節結構本身即為重點脈絡，**不放頂部三大重點 paragraph**，僅放摘要：

```
**摘要**：xxx
---
（正文）
```

**例外（社群貼文 social-post）**：貼文內容寫入「社群貼文」屬性欄位，頁面內文不寫稿。

寫入方式：餵 markdown 字串給 MCP，MCP 自動轉成上述 block 結構。**禁止手動建構 block JSON**——5/6 寫回時用 heading_2 + bullet 而非 paragraph 行內粗體，正是手動建 block 的踩坑案例。

**重要區別**：
- 寫入手法用 markdown 字串 ✅
- 最終 block 型別由 MCP 決定，不可控（例：markdown pipe 表格會被 MCP 自動轉成原生 table block）
- 若收到「不要原生 table block」需求，唯一選項是改條列（H3 + bullet 結構），**不要靠手動建 block JSON 強行 override MCP**

稿件全文一律用 `replace_content` 寫入頁面內文，不寫進長文屬性欄位。原因：`update_properties` 對含 Markdown code block 的長文有解析問題，會觸發 JSON 錯誤。

**步驟二：用 `mcp__claude_ai_Notion__notion-update-page`（update_properties 模式）寫入短欄位**

```
摘要       → 75 個全形字元以內（含標點），下限建議 ≥ 50 字以免事件交代不足；公式依稿件類型分流：
             • 即時新聞（news-daily）：以「把主要事件講清楚」為首要，覆蓋人／事／時／關鍵反差或結果，**不用「關鍵提問」結尾**（疑問句會稀釋事實密度）
               範本：「〔主體〕〔時間〕〔事件〕：〔關鍵細節 A〕，〔關鍵細節 B〕；〔反差或後果〕。」
             • 教學文／深度分析／潤稿：可用「核心題旨 + 關鍵提問」公式刺激點閱率
               範本：「〔主體〕為什麼〔張力點〕？因為〔核心洞察〕。〔具體收束〕。」
重點一     → 50 字以內
重點二     → 50 字以內
重點三     → 50 字以內
字數       → 計算頁面內文的字數後填入（數字）
狀態       → 「完成」
完成時間   → 當天日期
```

**注意：Notion 流程的稿件不存入 `draft/`。** Notion 頁面即為 single source of truth，不需要本地副本。`draft/` 僅用於非 Notion 流程的稿件（Terry 臨時交辦的產稿任務）。

**若來源網址無法讀取（403、付費牆、逾時），或子代理執行中發生錯誤：**

- 用 `replace_content` 寫入：`⚠️ 處理失敗，請人工確認。（原因：[說明]）`
- `狀態` 改為「失敗」

#### 2f. 寫回驗證

每篇稿件寫回 Notion 後，立即用 `mcp__claude_ai_Notion__notion-fetch` 讀取該頁面，確認：

1. 頁面內文非空（不是 `blank-page`，至少包含第一段內文）
2. `摘要`、`重點一` 等屬性欄位已有值

**若驗證失敗：**

- 重試 `replace_content` + `update_properties` 寫入一次
- 仍失敗 → `狀態` 標為「失敗」，在 Step 3 完成回報中列入警告
- 將稿件全文暫存至 `draft/`（作為備份避免內容遺失），更新 `draft/_register.md`。此為「Notion 稿件不存 draft/」的例外：技術故障時優先保全內容，待問題排除後再寫回 Notion 並刪除 draft/ 副本

**驗證範圍：** 每篇都驗，不抽樣。批次處理時寫一篇驗一篇，不等全部寫完再驗。

---

### Step 3：完成回報與 inbox 去留確認

所有條目處理完畢後，輸出摘要：

```
✅ 今日產稿完成

共處理 N 筆：
- 《標題一》：即時新聞 → 完成（查核通過）
- 《標題二》：深度分析 → 完成（查核已修正 2 處，1 處待編輯確認）
- 《標題三》：潤稿 → 完成
```

若有查核後需人工確認的項目，另外列出：

```
📝 查核標記（編輯請留意文中 [編按] 標註）：
- 《標題二》：某數據來源待確認、某引言出處待補
```

若有失敗或需人工介入的條目，另外列出：

```
⚠️ 需人工確認（N 筆）：
- 《標題》：來源無法讀取
```

#### inbox 檔案去留確認

若本次有任何來源為 inbox/ 本地路徑，在完成回報的同時，列出所有已消化的 inbox 檔案，附上預設建議，請使用者一次確認：

**預設建議邏輯：**

| 稿件類型 | 預設建議 | 理由 |
|---------|---------|------|
| 即時新聞 | 刪除 | 一次性事件，原始 URL 保留在 Notion 來源欄位 |
| 深度分析 | 移至 research/ | 公司／技術主題有累積價值 |
| 教學文 | 保留 inbox | 方法論素材可能再用 |
| 潤稿 | 刪除 | 草稿已轉為正式稿 |
| 社群貼文 | 刪除 | 來源文章已發布 |
| 文章查核 | 刪除 | 查核完成即結案 |

**輸出格式：**

```
📂 inbox 檔案去留：
1. OpenAI memo - The Verge.md → 建議刪除（即時新聞，已消化）
2. OpenAI memo - Gizmodo.md → 建議刪除（同上）
3. AI權力地圖報告.md → 建議移至 research/（深度分析素材）
4. Prompt教學法.md → 建議保留（教學文素材）

請確認，或逐項調整（例如「3 留 inbox」「1 移 research」）。
```

使用者確認後執行。移至 research/ 的檔案套用 research 標準格式，更新 `research/_index.md`。

**與 Wiki Ingest 的分工**：若 inbox 檔案已確認移至 research/，該篇素材的 Wiki Ingest 不再另建 research 筆記（避免重複）。Wiki Ingest 仍可為同一篇稿件涉及的**其他**公司、人物、技術主題建立獨立筆記。
