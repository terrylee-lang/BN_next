# 《數位時代》統一風格規範

此文件為跨 Skill 共用的寫作風格規範，由各 Skill 在執行前以 `Read` 工具載入。

**適用範圍**：本規範不限「產稿 Skill 執行時」。凡是 Claude 產出、會寫進 vault 或 memory 的 Markdown 檔案（research 筆記、projects/、archive/、daily-notes/、memory/、自動化寫回 Notion 的稿件等），一律適用本文件所有規則：兩岸用語、人名公司名、禁用詞彙、破折號零容忍、技術白話化。在對話中即時生成、不存檔的文字可放寬破折號規則，其他規則仍要遵守。

---

## 兩岸用語轉換（必須執行）

| 大陸用語 | 台灣用語 |
|---------|---------|
| 關停 | 關閉／停止營運 |
| 光刻機 | 曝光機 |
| 芯片 | 晶片 |
| 視頻 | 影片 |
| 固件 | 韌體 |
| 算法 | 演算法 |
| 屏幕 | 螢幕 |
| 特朗普 | 川普 |
| 質量（品質義） | 品質 |
| 数据 | 資料 |

**SI 單位前綴譯法**（避免 tera/mega 混淆）：

| 英文 | 台灣標準譯法 | 說明 |
|------|-------------|------|
| tera-（T，10¹²）| **太**（太瓦 TW、太赫茲 THz） | 不用「兆瓦」——大陸語境「兆」常指 mega-（MW），易被誤讀 |
| giga-（G，10⁹）| **吉**（吉瓦 GW、吉赫茲 GHz） | 避免用「十億瓦」這類囉唆譯法 |
| mega-（M，10⁶）| **百萬**（百萬瓦 MW） | 避免用「兆瓦」指 MW |

遇到 SI 單位時，**優先保留英文縮寫**（TW、GW、MW、THz），第一次出現時附中文：例「1 TW（太瓦）」。

---

## 人名與公司名規範

**公司行號名稱**：
- 有官方中文名稱者，**一律以中文名稱為主**，英文原文以括號附註於第一次出現時
  - 例：大鉦資本 (Centurium Capital)、輝達 (NVIDIA)、雀巢 (Nestlé)
- 無官方中文名稱者：保留英文原文，不強行音譯或意譯

**人名**：
- **絕對禁止**依外媒英文拼音自行音譯中文人名，錯誤示範：Li Hui → 李輝（實為黎輝）
- 查證順序：① 當事人官方資料或公司公告 ② 台灣主流媒體已使用之譯名 ③ 中國官方或當事媒體
- 查不到可靠中文名稱時：**保留英文原名**，並在文末或編輯附註中標記「人名待確認」
- 有公認譯名者：第一次出現使用「中文譯名 (英文原文)」格式
  - 例：伊隆·馬斯克 (Elon Musk)

---

## 日期格式

**月日**：寫成「4 月 27 日」，**不用斜線格式**（例 ❌「4/27」、「10/9」）。

**年份**：「2026 年」。

**完整日期**：「2026 年 4 月 27 日」。

**日期區間**：同月份「6 月 1 至 5 日」；跨月份「5 月 30 日至 6 月 2 日」。

**相對時間**（唯一判準：**見報後是否失效**）：

- **永遠禁用**（以產稿日為基準，見報即失效）：「今日」「明日」「昨日」「稍晚」「即將」「目前」「上週」「本月初」等，一律改絕對日期（「4 月 27 日」）或加「截至 X 月 X 日」。完稿後全文搜一遍這類詞
- **允許保留**：不隨見報日漂移的寫法（「今年第一季」「發表會當天」「公告後一週內」）
- 引用來源中的相對時間，換算為絕對日期後查證，不可用「上週」自行推算日期

**例外（不適用本規範）**：

- URL、frontmatter `created`／`updated` 等機器讀取欄位，沿用 ISO 格式（YYYY-MM-DD）
- 財報季度標記（1Q26、Q1 2026、FY2025）保留原格式
- 圖表、原始文件、官方公告的標題引述（直接引述時保留原貌，但在敘述句中提到時應換為中文格式）

---

## 禁止出現的寫法（AI 味句式總表，全 Skill 唯一版本）

本表為所有產稿 Skill 的 canonical，deep-analysis、draft-polish 等不再各自維護副本。draft-polish 的「前兩段 Hook 例外」見該 Skill。

| 類型 | 例子 | 上限 |
|------|------|------|
| 空洞開場白 | 「不可否認」、「毫無疑問」、「顯而易見」 | 全文 0 次 |
| 「值得注意的是」 | 承載真實提醒（限制條件、易誤讀處）時可用；不得作為無資訊量的段落起手式 | **每篇最多 1 次** |
| AI 式總結句 | 「這一切都指向一個事實：」、「歸根究柢，問題在於……」 | 全文 0 次 |
| 行銷形容詞 | 「革命性」、「顛覆性」、「里程碑」、「劃時代」（無具體數據依據時） | 全文 0 次 |
| 翻譯腔 | 「於此同時」作為固定句型、「對此」作為段落起手式 | 全文 0 次 |
| 空指稱弱化詞 | 「可以說」、「某種程度上」、「在多數情況下」 | 全文 0 次（必要時以條件限定「若…則…」替代） |
| 過度比喻 | 把技術比喻成「引擎」、「基石」、「里程碑」等老套說法 | 全文 0 次 |
| 同義反覆 | 「重要且關鍵」、「快速且迅速」、「影響並衝擊」 | 全文 0 次 |
| 誇飾轉折 | 「然而，這僅僅是開始……」、「更令人震驚的是……」 | 全文 0 次 |
| 對比句式（不是…是） | 「不是技術問題，是策略問題」 | 每 600 字最多 1 次；不得用於連續段落開頭；不得與排比同段併用 |
| 排比與三段式 | 連續三句結構相同的列舉 | 每 600 字最多 1 次；子項不超過 3 個；子項語意不可重複或空泛 |
| 反問設問 | 「這究竟意味著什麼？」 | 每 600 字最多 1 次；不得連用兩個以上；反問後需緊接具體答案或依據 |
| 括號旁註 | 用括號放入關鍵論點 | 僅補充術語、數據或範圍；不得承載關鍵論點 |
| 破折號 | **全面禁用，適用所有稿種**（新聞、深度分析、教學文、潤稿、社群貼文、ETF 懶人包，含標題、摘要、圖表文字）：全形破折號「——」與半形 em dash「—」一律不用，需要停頓或補充說明時改用逗號、句號或括號。**本表為破折號規則唯一所在地**。自查指令統一為 `grep -cE "[—－]"`（涵蓋全形破折號 U+2014 與全形減號 U+FF0D），期望值 0。唯一例外：對話中即時生成、不存檔的文字可放寬（見本檔開頭適用範圍） | 全文 0 次 |
| 重複觀點與過度細節 | 同一個判斷或心得在不同段落換句話再說一次；操作／實測過程把工具的中間交付、失敗重試、除錯往返逐一寫出；同一功能的優點列舉超過三項 | 同一觀點全文只出現一次；實作過程只寫到讀者能重現（保留使用者下的 prompt 與調整原因，刪工具中間交付的細節）；功能列舉每項一句、不超過三項。這是 2026-08 糾正量第二大的類型（12/34）：兩期會員專刊共 5 筆「太細／廢話／精簡 15%」，同月 OpenClaw 教學文被批「整篇都在廢話，重寫算了」 |

**遇到問題句型的替代寫法**：

| 檢出句型 | 改寫方向 |
|---------|---------|
| 重複觀點／過細的實作過程 | 同一判斷只留第一次出現處，其餘刪除或併入；實作段改成「做了什麼→看到什麼→調整了什麼」三句一組，工具的中間交付與除錯往返不展開 |
| 「不是 A，是 B」 | 改為「核心重點在於 B；A 屬於次要面向」 |
| 三段式排比 | 若子項雷同，合併為一段自然敘述，保留唯一必要重點 |
| 反問句 | 改為陳述式：「主因為⋯⋯，具體表現為⋯⋯」 |
| 破折號連用 | 將補充資訊拆為獨立句，或以條件限定另起一句 |

---

## 正文字數（全域規則）

- **全稿種正文上限 3,500 字**（中文字數計）。各 Skill 的字數區間是「目標」不是各自的上限；唯一硬上限就是本條
- **唯一例外：ETF 規格懶人包（`etf-explainer`）3,500–4,500 字**（SEO 流量文，9 個 H2 + Q&A，2026-07-29 Terry 確認保留）。例外只此一支，其餘稿種要放寬一律先問，不得自行援引本條
- **預估成品將超過 3,500 字時，必須在大綱規劃階段向使用者警示**，由使用者決定拆稿、壓縮或放行，不可寫完才回報
- **多產品彙整型 roundup**（如「微軟 Build 新品一次看」）：正文上限 2,000 字，預設往 1,700–1,900 字寫，不因產品多而放寬。壓縮時優先砍重複的白話比喻與轉場鋪陳，保留產品小節結構、所有關鍵數字、階段限制語氣。（案例：2026-06-03 微軟 Build 懶人包擴到 2,674 字，Terry 裁定「上限調成最多 2,000 字」。）
- 白話化導致的字數放寬見「技術性稿件白話化原則」字數彈性處理，仍受本節 3,500 字上限約束

---

## 技術性稿件白話化原則

**何時啟用**：當稿件主題涉及 AI 模型、程式開發工具、晶片架構、雲端基礎設施、API 變更、資安技術、通訊協定、技術標準等，**預設啟用白話化**，以照顧非技術背景的商業讀者。唯一例外：備註欄明確指示「針對開發者讀者」或類似語句時，可保留技術深度。

**五條具體原則**：

### 1. 技術名詞第一次出現時加中文白話解釋

英文術語、benchmark 名稱、產品代號第一次出現時，用「英文（中文白話說明）」格式。後續可直接用英文。

| ❌ 技術腔 | ✅ 白話化 |
|---|---|
| SWE-bench Pro 解題率從 53.4% 升至 64.3% | SWE-bench Pro（軟體工程實測）解題率從 53.4% 升至 64.3% |
| tokenizer 更新導致 token 數增加 | 換了一套處理文字的方式（業界稱為 tokenizer），token 數增加 |
| adaptive thinking 預設為關閉 | 預設不啟用「邊想邊做」的模式（官方稱 adaptive thinking） |

### 2. 小標改問句或大白話，避免直接堆疊技術詞

讀者會從小標決定要不要看下去，技術詞堆疊會嚇跑非技術讀者。

| ❌ 技術腔小標 | ✅ 白話化小標 |
|---|---|
| benchmark 數字對比 | 實際進步多少？幾個關鍵測試成績 |
| 新功能：xhigh 推理、task budgets 與 /ultrareview | 同步推出的新工具：可調思考深度、設花費上限 |
| 開發者須知：tokenizer 變更與 API breaking changes | 企業升級要留意：費用會變貴、舊 prompt 要重寫 |
| Mythos 安全實驗場 | 也是 Anthropic 新安全機制的「第一個白老鼠」 |

### 3. 技術細節壓縮為「對讀者的影響」，不堆程式碼

API 參數名稱、code block、錯誤碼這類內容，**不直接列給一般讀者看**，改為敘述影響面。若需要完整技術清單，在段末加「詳細清單見官方遷移指南」連結即可。

| ❌ 直接列技術細節 | ✅ 敘述影響面 |
|---|---|
| `thinking: {type: "enabled", budget_tokens: N}` 會回傳 400 錯誤，需改用 `thinking: {type: "adaptive"}`；`temperature`、`top_p`、`top_k` 非預設值也會回 400 | 舊版 API 參數已移除，用舊寫法會直接回報錯誤，開發者須改寫為新格式。詳見官方遷移指南 |

### 4. 用具體例子取代抽象百分比／倍率

讀者對「1.0–1.35x」「多三倍」沒感覺，用實際情境讓數字變具象。

| ❌ 抽象 | ✅ 具象 |
|---|---|
| token 數增加 1.0–1.35x | 原本一篇文章花 1,000 token，升級後可能要花 1,350 token，帳單自然上升 |
| 視覺 token 三倍 | 一張完整解析度的圖最多吃掉 4,784 個 token，是舊模型 1,600 token 上限的約三倍 |

### 5. 行為差異用白話翻譯，不用官方語言直譯

官方技術文件的英文原文照翻通常很拗口，翻成「這句話的意思是……」效果最好。

| ❌ 官方直譯 | ✅ 白話翻譯 |
|---|---|
| 對指令的遵循明顯更嚴格（more literal instruction following） | 過去會自動腦補、寬鬆詮釋模糊指令，新版則完全照字面執行 |
| 響應長度依任務複雜度自適應 | 簡單問題回覆更短、複雜分析回覆更長，不再固定 |

### 6. 開頭從讀者利益切入，結尾用「先 X，再 Y」遞進收束

技術稿與方法論稿的開頭與結尾結構，直接決定讀者願不願意讀完。

**開頭**：不從「某公司宣布⋯⋯」或「某 CEO 主張⋯⋯」這種權威角度切入，從「對讀者有什麼好處／意味著什麼」切入。

| ❌ 權威切入 | ✅ 讀者利益切入 |
|---|---|
| Anthropic 5 月發表《創辦人劇本》，主張創辦人正在從「執行者」轉為「協調者」⋯⋯ | AI 對創業者最大的幫助，不只是「幫你寫程式」。更大的改變是：很多原本要靠團隊、顧問或外包才能完成的事，現在可以先交給 AI 做第一輪。 |
| Google 在 Cloud Next 大會上發表新 TPU⋯⋯ | 如果你的公司每月 AI 帳單超過 10 萬美元，這項新晶片可以讓成本降到原本的⋯⋯ |

**結尾**：方法論稿、教學稿的收束用「先 X，再 Y；先 X，再 Y」遞進句式，讓讀者讀完能立刻記住流程。比文學比喻更有記憶點。

> 範例：「先驗證問題，再做 MVP；先寫清楚範圍，再讓 AI 加速開發；先把營運流程拆開，再交給工具協助；最後，才把累積下來的知識變成護城河。」

**避免文學成語／軍事用詞，要白話**：

| ❌ 文學／軍事用詞 | ✅ 白話 |
|---|---|
| 事必躬親 | 親力親為 |
| 指揮系統 | 指揮 AI 工具 |
| 披荊斬棘、力挽狂瀾、紛至沓來、有條不紊 | 改寫成具體場景敘述 |

讀者不是 MBA 教科書，越白話越好。出稿前 grep 一遍常見文學成語，能改白話就改。

**白話轉場詞節制**：「白話說」「換句話說」「也就是」這類轉場詞，**同一轉場詞單篇不重複使用**（第二次起換不同變體，或直接讓解釋句自己站住），不設總次數上限。出稿前 grep 自查是否有同一轉場詞出現兩次。

---

**核心檢查**：稿件完成後，自問「如果讀者不是工程師、沒用過這個工具，他看得懂我在說什麼嗎？」看不懂的段落就要改寫。

**字數彈性處理**：白話化通常會比技術直譯略長（加了中文解釋、具體例子）。當白話化讓稿件超出原 Skill 的字數目標（例如 news-daily 的 1,000 字），**以「讀者能看懂」為優先**，允許超出目標約 30% 到 60%（例如 1,000 字稿件可放寬至 1,300–1,600 字）。超出更多時，應回頭檢視是否有冗段可刪，而非犧牲白話化。全稿種 3,500 字上限（見「正文字數」節）不因此放寬。

---

## 正文首段獨立閱讀原則

「重點一二三」「摘要」是後台欄位鏡像，**不算正文**；讀者讀正文時未必先讀到它們。正文真正的第一段必須能獨立成立，三項檢查：

1. 主題／人物首次提及就交代清楚（這是誰、在講什麼、為何在意），不依賴摘要欄當背景
2. 不可在正文正式介紹人物之前就用姓氏熟稱（先點名還沒登場的人＝順序顛倒）
3. 不用內行例子冷開場；場景與論點前移，例子降為佐證

（案例：正文首段寫「Shihipar 舉例……」，但此人到第二段才被正式介紹，讀起來像從中段抽出的片段。）

---

## 他國市場金額的取捨

編譯外文素材遇到當地市場價格時，判斷分界是：這個金額是讓台灣讀者**理解世界發生什麼事**，還是**期待讀者套用自身情境**？

- **保留並換算**：跨國企業估值、AI 服務訂閱方案、全球參考薪資水準、鉅額投資數字
- **直接刪除或改概括說法（不做匯率換算）**：美國個別服務定價（營養師諮詢、健身餐訂閱、家庭餐飲預算）、在地消費水準金額。與台灣市場結構脫節的金額換算成台幣，反而讓讀者誤以為可套用自身情境，可信度打折

動筆前先掃一次原文金額清單做此判斷。（案例：2026-04-21「Claude 營養師」教學文，Terry 裁定美國餐費換算應整段刪，不是補匯率。）

---

## 正文的來源歸屬分界（跨 Skill 共用）

媒體報導在流程裡有兩個身分：**查核階段的交叉驗證依據**，以及**正文裡的出處歸屬**。兩者不可混用。公開可查的客觀數據直接敘述，掛上媒體名稱等於暗示這個數字是「某家媒體的說法」，讀者會誤以為它要靠該媒體的信用才成立。

**直接敘述、不掛媒體名稱**（任何人查得到，與哪家媒體報導無關）：

- 股價與市值：盤中漲跌幅、收盤漲跌幅、單日漲幅紀錄、市值、成交量
- 公司自己公告的財報數字：營收、獲利、EPS、部門營收與成長率、財測與資本支出金額
- 政府或交易所公布的數據：匯率、指數報酬、公開統計
- 法院文書、SEC filing、官方公告裡明載的事實

**必須掛出處**（拿掉出處就變成本刊自己的斷言）：

- 該媒體的詮釋、歸因與判斷（「這波賣壓來自投資人對 AI 資本支出失去耐性」）
- 該媒體專有的資料彙整或自訂指標（Bloomberg 彙整的分析師平均目標價、它自訂的七雄指標、它算出的本益比對十年均值折價）
- 分析師預估值、券商目標價、第三方機構的調查或共識數字（StreetAccount、Visible Alpha 等）
- 獨家報導、未經官方確認的消息（證據級別依 article-checker 規則 E 判定）
- 引言：引述規範見各 Skill，二手轉述必加「（XX 媒體引述）」

判準一句話：**這項資訊被質疑時，靠得住的是原始事實本身，還是那家媒體的信用？** 前者直述，後者掛名。

（案例：2026-08-04 亞馬遜 3 兆美元市值稿寫成「據《Bloomberg》報導，當天盤中一度大漲 5.3%，《CNBC》報導終場收漲 4%」，Terry 指出股價是客觀數據、媒體只該是查核依據，「在股價上引用其他媒體根本毫無邏輯」。同稿另有兩處同型錯誤：亞馬遜自家財報的營收與 EPS 掛《CNBC》、微軟與 Alphabet 自家財報公布的雲端成長率也掛《CNBC》。）

---

## 來源讀取優先序（跨 Skill 共用）

**進入下列流程前，先確認有沒有素材需要主動找**：使用者只給題目或口頭交辦、訊息裡沒有具體路徑／連結／Notion 來源欄位時，先用題目關鍵字掃一輪 `~/Claude Project/inbox/`（`ls` 檔名、`grep` 內文皆可，不只是比對 frontmatter `source`）——Terry 的慣例是素材先丟進 inbox 才下產稿指令，訊息裡不會每次重複點名「inbox 那篇」。inbox 有相關檔案就當作指定素材優先讀取；掃過確認沒有，才進入下列 web_fetch／web_search 找新資料。漏這一步、直接對外找資料，會用到立場或細節不同的替代素材，長文 grill 大綱、甚至已完稿的稿件都可能因此在讀到 inbox 原始素材後重工（2026-09-04 踩坑：longform 大綱先出、後來才發現 inbox 早有指定素材）。

各產稿 Skill 取得已指定或已尋獲的素材時依序嘗試，成功即停止：

1. **本地檔案路徑**（`/` 或 `~/` 開頭的 `.md`）→ `Read` 直接讀取
2. **URL → 先掃 `~/Claude Project/inbox/`**：比對所有 md 檔 frontmatter `source`（嚴格相等優先；其次 hostname + path，忽略 query string、fragment、尾端斜線），命中則 `Read` 本機檔全文
3. inbox 未命中 → `web_fetch` 讀取全文
4. 失敗（逾時、403、空白、付費牆）→ **立即停止並回報使用者**，索取原文或更換來源，不可自行替換素材繼續產稿
5. X／Twitter 連結 → 直接告知無法讀取，請使用者貼入貼文文字
6. 直接貼入的文字直接作為素材；素材可讀但背景不足時，可用 `web_search` 補充並在資料來源區標明

**不使用 Playwright 作為內容讀取備援**：snapshot 吃大量無關 DOM，浪費 token 且品質差。Playwright 僅用於需互動操作的頁面。

**付費牆媒體**（Bloomberg、WSJ、日經、Business Insider）：Terry 有付費帳號與 Dia 瀏覽器。`web_fetch` 失敗時直接索取原文，不嘗試繞過、不用摘要或二手引述硬寫。

**PDF**：先用 `pdftotext`（在 `~/.local/bin`）萃取純文字到 scratchpad 再讀；需看版面／圖片才用 Read 的 pages 參數（單次 ≤5 頁）。不讓大型 PDF base64 常駐對話。

各 Skill 的特殊輸入（YouTube 字幕、Podcast 逐字稿）依各 Skill 補充規則處理。

---

## 事實查核鐵律

**適用**：所有產稿 Skill（news-daily、deep-analysis、tutorial-article、draft-polish、etf-explainer）在**產稿階段**就要遵守，不能只靠 article-checker 事後兜底；article-checker 查核時逐項對照。social-post 與 headline-generator 非全文產稿，僅適用本檔的用語、人名與禁詞規範，不適用本節流程性條款。以下每條都附真實踩坑案例，作為判斷錨點。

### 一、來源紀律

1. **讀不到就停**：來源（官方新聞稿、付費牆 Bloomberg／WSJ／日經／Business Insider）讀不到時，回報失敗並向 Terry 索取原文（Terry 有 Dia 瀏覽器可擷取），**絕不拿 PYMNTS、TestingCatalog 等二手來源拼湊**。（案例：2026-04-10 三篇稿引言被改寫、McGuirk 引言憑空捏造、75% 數據來源錯誤、軟銀 400 億貸款不存在。）
   - **「讀到」的定義：摘要不算。** `web_search` 回傳的搜尋摘要、`web_fetch` 由小模型生成的頁面摘要，只能用來判斷「這個來源值不值得讀」，**不得作為引用依據**。要寫進稿子的數字、引言、時序，一律回原文核對；原文過長時用 `Grep` 或章節標題定位再讀該段，不必整篇塞進對話。摘要會靜默壓縮數量級與語意層級，而且錯在摘要裡看不出來。本條與規則 3 互補：規則 3 要求去找一手，本條定義找到之後怎樣才算讀了。（案例：2026-07-30 Boris Cherny 長文，Bun 改寫的工作流數量先照搜尋摘要寫成「約 50 個」，察覺要查一手後又照 `web_fetch` 摘要「修正」為 4 個，把對的改成錯的；一手全文寫的就是約 50 個，4 個是尖峰同時併發數。同篇另有「1,600 個編譯錯誤」實為約 16,000 個，且「12 小時內清完」整句虛構。一手來源 Jarred Sumner 的技術回顧全程公開可讀，卻到 Terry 把檔案丟進 inbox 才真正讀完。）
2. **無幽靈段落**：正文每段事實都要能回溯到「資料來源」區列出的素材；web_search 補的背景也要同步標進來源區。（案例：黃仁勳稿一整段 H200 出口許可來自外部報導，來源區只列 Podcast，最終整段刪除。）
   - **素材優先於結構，不先射箭再畫靶**：各 Skill 的「必答」「必須有這一節」「全文至少一個 X」，以及稿單備註列的項目，一律是**檢查提示**（提醒你回素材找），不是生產指令。回去找了、素材撐不住，正解是**在編輯附註寫明「素材無此面向，略過」**，略過不算缺陷。硬補一段會同時製造兩個問題：查核時在素材裡找不到出處，只能標「無法核實」，等於自己埋查核破口；段落與主軸沒有真正的關係，讀感也散掉。**備註同樣適用**：它寫的是編輯希望文章長什麼樣，不保證素材撐得住，撐不住就回報「備註要求的 X 素材裡沒有」，不自行生成。（案例：2026-08-03 Whatnot 產品長 PM 稿，為滿足 `longform` rubric 的「台灣關聯」必答項，生出「台灣科技業職級與薪酬區間普遍壓縮、資深產品人才供給更薄」這種零查證支撐的句子，Terry 要求整節刪除。）
3. **一手素材覆蓋**：單一事件（併購、融資、產品發表、官方公告）產稿前必查當事公司官方 X／部落格／新聞稿／SEC filing，不能只靠編輯丟進 inbox 的二手 clipping。（案例：SpaceX 官方推文的「million H100 equivalent Colossus」算力規格被漏用。）

### 二、數字與數據

4. **時效財務數字必查最新值**：ARR、年化營收、估值、WAU／MAU、融資輪金額、市值等過去 12 個月快速變動的指標，不論素材多權威，都要另外 web_search 抓 (a) 當事公司最新公告、(b) 第二個獨立來源交叉驗證。二手報導引用的里程碑常超連結連回數月前舊稿。（案例：Cursor ARR 盲信 NYT 引用的 2025-02 舊里程碑「不到兩年衝上 1 億」，實際已跨 20 億，差 10 到 20 倍。）
5. **財報數據逐字確認欄位**：每引用一個數字，回頭確認 (1) 出自哪一欄、(2) 欄位時間區間、(3) 是 Guidance 還是 Actual、QoQ 還是 YoY。素材沒有某區間數據就不寫，不用其他欄位代替。（案例：台積電簡報「1Q26 Guidance」被誤讀為 Q2 展望，虛構「第二季財測營收 346-358 億美元」。）
6. **「約」字近似值不可反推精確值**：原文寫「約 X」時該值已是四捨五入結果，不能拿去精算另一個未標「約」的數字，四捨五入累積誤差會讓你錯判作者寫錯。標「可疑」前先問：這個差異是否恰好等於所有「約」字累積的小數遺失？（案例：南亞科私募把「約 1.39 億股」等近似值相加乘單價，誤稱與作者 787.18 億差 0.7 億，實際完全吻合。）
7. **調查數據兩鐵律**：(a) 樣本／時間／問題不同的兩份調查不可併在同一句，逐句確認出自哪一份；(b) 不可用「X% of N」自行推算絕對數（「90% 的 20,000 名開發者」不寫成「約 18,000 人」）。（案例：Pragmatic Engineer 與 JetBrains 兩份獨立調查混用、Cursor 90%×20,000 自行推算。）
8. **結構化資料庫只寫實際欄位**：用 MCP／API／Notion／政府開放資料拉到資料後，只在實際回傳的欄位範圍內描述，不引用不存在的欄位、不對欄位內容做未經查核的延伸詮釋。要補背景必須標「web 補充查證」並另跑 web_search。（案例：立委資料無「不分區排序」欄位、「離職原因」只寫「辭職」，卻自行加註「原排序 1、轉任院長」兩個事實錯誤。）
9. **【重訊高危】占比／金額語意辨識**：重大訊息中「占總資產比例」「占母公司業主權益比例」「累積持有金額」等，若欄位附近有「迄目前為止」「累積」「（含本次交易）」等限定詞，即為**累積值不是單筆值**。引用必帶前提詞（「含本次交易在內，累積已占…」），且做常識檢查（交易金額 ÷ 公司總資產粗算單筆占比是否合理）。對一般讀者價值低又易誤讀時，**建議直接刪除整段比例敘述**。核心是語意辨識不是欄位編號。（案例：統一超 20.54 億占千億級總資產約 1-2%，卻把累積 36.35% 誤寫成「本次投資占總資產 36.35%」，已造成統一超公關抗議。）

### 三、用詞精準（不語意升級）

10. **商業動詞精準**：acquire／收購、license／授權合作、invest／投資、partnership／合作，性質完全不同，翻譯前先確認原文動詞，不憑語感選詞，不把語意較輕的升級為較重的。（案例：Groq 的「授權合作」被寫成「收購」。）
11. **技術術語不戲劇化升級**：zero-day exploit／零日漏洞 ≠ security vulnerability／安全漏洞；secure relay 不擴寫成「加密通道」、sandboxed execution 不擴寫成「Docker 隔離」。原文用什麼字就用什麼字，看不懂走白話化「解釋」（見技術白話化原則），不是「擴寫」成另一個技術名詞。（案例：「安全漏洞」被升級為「零日漏洞」。）
12. **SEC 文件分類**：S-1／S-1/A = IPO 招股書／註冊聲明書（**不是財報**）；10-K = 年報、10-Q = 季報（才是財報）；8-K = 重大訊息公告；DEF 14A = 股東會委託書。IPO 階段稿件用「招股書」不用「財報」，視覺配件 kicker／標題用詞精準度等同正文。（案例：SpaceX S-1 配件寫成「財報拆解」。）
13. **禁自行推斷比較級**：「首度」「史上首次」「首破」「創新高」「最快」「最大」等，除非來源明確使用，否則不寫。從圖表目測、有限數據推論的判斷（趨勢、比較級、因果）若素材沒直接寫，加 `[編按：此為推論，請查核]`，不靜默寫入。（案例：台積電只有三季數據就寫「淨利率首度突破 50%」、從長條圖目測「3 奈米成長最快」。）
14. **未成交事項保留軟化語氣**：target／targeting／expected／plans to／in talks to／reportedly／proposed 對應「目標／預計／洽談中／據傳／外電預期」。未上市估值以區間或「約」表示；併購未交割寫「取得收購選擇權」「協議以 X 收購」，不寫「買下」。（案例：Bloomberg「targeting a $2 trillion valuation」被升格為「SpaceX 目標估值超過 2 兆美元」，共識實為 1.75 到 2 兆區間。）

### 四、官方公告型素材六項 pre-check

編譯單一公司官方公告（OpenAI feature drop、Apple 產品發表、Google blog post）時，動筆前逐項檢查（article-checker 容易漏這類語意問題）：

(a) **日期**逐字抓官方頁面 publish date 字串貼回稿件，補台灣時間轉換，不用「最近」推算、不套用該公司另一場發表的日期；(b) **產品架構 verb 對應**，control from／access from／monitor／approve from 譯成「遠端存取／審核／批准」，不升級成「跑在手機上」「進駐手機」；(c) **平台支援度分流**，逐項標明 GA／preview／coming soon／Enterprise only，不把「Windows coming soon」寫成「全平台支援」；(d) **技術名詞照字面**，不擴寫（secure relay 不寫「加密通道」）；(e) **合規語氣保守**，HIPAA／GDPR／SOC 2 等「helps you meet requirements」不等於「you can now process X」；(f) **旁支新聞砍乾淨**，公告底部順帶提到的次要訊息若非本次主體就刪，要保留須另查獨立來源。（案例：OpenAI Codex 手機版稿被抓七個失準點，把 5/14 誤寫 5/7、「遠端介面」寫成「進駐手機版」、漏 Windows coming soon、secure relay 擴寫成加密通道、HIPAA 寫成「可接病患資料」、結尾混入未查證的 Pro 方案 100 美元與 GPT-5.5。）

本節是六項 pre-check 規則的**唯一 canonical**；觸發條件與操作級對照表（verb 對應表、擴寫陷阱表、分階表、合規對照表、日期提取細節）見 `news-daily.md` Step 2c，該處為操作層不另立規則。

### 五、流程紀律

15. **近期併購／授權／人事先 web_search**：查核 AI／半導體／晶片／雲端領域的「收購」「授權」「併購」「挖角」「策略聯盟」時，標編按前先 web_search 確認「此事不存在」再下判斷，不可僅憑訓練知識庫「沒聽過」就標錯（訓練資料截 2026-01，2025 年底新聞收錄不完整）。質疑產業專欄作家（吳金榮、王志仁等）的事實前，舉證責任在自己。（案例：NVIDIA 2025-12-24 確與 Groq 簽非獨家推論技術授權，卻被誤標編按為作者筆誤。）
16. **實測／demo 未實操禁止假敘事**：使用者還沒實際操作的實測／demo／walkthrough，禁止用第一人稱過去式寫結果（「我花了 35 分鐘、用掉 20 個 prompt」），改用「建議這樣做／建議嘗試的類型 A／B／C」模板語氣，截圖處留 `[截圖位置：XXX]`。（案例：Claude Design 教學文被寫成虛構的 35 分鐘、20 prompts、三個假 comment、三個假 slider 名稱，被 Terry 嚴厲糾正。）
17. **查核不分來源、改寫後補跑**：所有產稿完成、成品推到 draft/ 或 Notion 後一律跑 article-checker（錯字、邏輯、事實、貨幣換算），不分 inbox／Notion／research／hybrid。經多輪改寫（摘要、重點、譯名統一、段落、白話化）後**主動補跑**，不等 Terry 追問。全文字串替換（譯名統一等）時特別檢查引言歸屬，不假設替換工具已處理事實層。（案例：inbox→Notion 三篇誤以為「走 Notion 有內建查核」全漏跑；Google CapEx 稿五輪改寫沒重跑，一次抓出 13 處問題含皮蔡引言歸屬錯誤。）
18. **已查證的錯誤直接修**：已經 web_search／Read 來源／跑過 article-checker 確認是事實錯誤的，直接改、改完回報，不用「要不要改？」把判斷推回給 Terry。只有呈現方式的取捨（保留費率欄位加編按 vs 移除）才是風格選擇、需要徵詢。（案例：00403A 懶人包已查證「0050 內扣 0.22%」是舊數字、持股上限對比有誤，卻問「兩個都改還是只改一個」，Terry 回「錯誤有需要不修改的嗎？」）

---

## 署名格式

文末「資料來源 + 署名」為固定格式，**兩行都必須存在、缺一不可**：

```
---

資料來源：[Anthropic 官方公告](URL)、[《TechCrunch》](URL)、[Anthropic 系統卡](URL)、[Morph SWE-bench Pro 榜單](URL)、[Anthropic 定價](URL)

本文初稿為AI編撰，整理．編輯/ 李先泰
```

規則：

- **資料來源寫成同一行**，各來源之間用頓號「、」分隔，名稱一律加 Markdown 超連結 `[名稱](URL)`。不裸露 URL、**不使用條列 `-`、不加 `## 資料來源` 小標、不使用英文「Sources:」**
- **名稱慣例**：新聞媒體用《》（《TechCrunch》《路透》《CNBC》）；官方公告、系統卡、定價頁、開發文件、榜單等用描述性名稱、不加《》（Anthropic 官方公告、Anthropic 定價、Gemini 定價、Morph SWE-bench Pro 榜單）
- 多來源時，正文引用到的所有媒體與官方頁面都要列出
- **署名行固定為「本文初稿為AI編撰，整理．編輯/ [責任編輯]」**：責任編輯姓名 = 該稿負責人的署名全名（Terry 本人稿件為「李先泰」；Notion 流程取自「負責人」欄位）。此行文字不可改寫、不可省略

⚠️ **多輪查核／改寫後必須原樣保留此文末格式。** 實務上稿件經 article-checker、orchestrator 寫回等多手後，資料來源常被改成條列或英文「Sources:」、署名整行遺失（2026-07-01 先泰三篇即發生）。任何改寫、查核、寫回動作的**最後一步**，都要確認文末仍是「單行頓號資料來源 + 署名行」兩行俱全，格式如上。

**這一行是結構化資料，不是排版。** 推送 Pluto 後台時會解析成 `content-pool-add` 的 `reference` 參數（`[{"title":"","url":""}]`）：取 `資料來源：` 之後整行，以「、」切開，每段 `[名稱](URL)` 的名稱作 `title`、URL 作 `url`。改成條列、加小標、裸露 URL 都會解析失敗。

`reference` 只取這一行，**不取 Notion「來源 1／2／3」欄位**：那三欄是 AI 讀的素材入口，這一行是給讀者的引用清單，兩者不等價（`web_search` 補的來源在這一行不在那三欄，讀了沒引用的則相反）。

---

## 文章元件語意標記（推送 Pluto 用）

後台把抽言、延伸閱讀等富媒體存成 plugin 區塊。稿件層**不寫 plugin JSON**，改用下列標記，推送層機械轉換。標記獨立成段、行首粗體、全形冒號，同「**摘要**：」形狀（已驗證通過 Notion MCP 寫回不變形）。元件皆為選用，要寫就必須照下表，推送層不做猜測。

| 元件 | 標記 | plugin | 規則 |
|---|---|---|---|
| 抽言 | `**抽言**：引文（出處）` | essence | 引文不加引號（後台樣式自帶）；出處放結尾全形圓括號，無出處則整個省略。引文進 `description`、出處進 `from` |
| 延伸閱讀 | `**延伸閱讀**：[標題](URL)、[標題](URL)` | inlinelinks | 解析規則同資料來源那一行。`description` 留空 |
| 重點框 | `**重點框**：內容` | textblock | 一律 `style:"default"`。plugin 另有 type1 到 type6，後台尚未提供視覺說明，取得後改為 `**重點框（type2）**：` |
| 影音 | `**影音**：[標題](URL)` | video | 限 youtube／vimeo／dailymotion／ted／giphy。`site` 由網域判定、不由編輯指定。稿件內不寫原生 `<iframe>` |

圖片與圖說未納入：`picture` 的 `src` 取自 `media-upload` 回傳的 object（已確認），但《數位時代》的 site 值尚未由後台提供，故仍維持人工處理。

推送層規格（plugin 轉換、HTML 隔離、已確認裁決與待補項）見 `bwt-pluto-push-spec.md`，要推內容池或產文章內嵌 HTML 時載入。

---

## Notion 寫回內文格式（news-daily／deep-analysis 共用）

若需寫回 Notion，`replace_content` 的內文區塊格式依序為：

```
## 標題

**重點一**：...
**重點二**：...
**重點三**：...

---

**摘要**：...

---

### 小標（正文開始）
...
```
